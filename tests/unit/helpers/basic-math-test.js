import { basicMath } from '../../../helpers/basic-math';
import { module, test } from 'qunit';

module('Unit | Helper | basic math');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = basicMath(42);
  assert.ok(result);
});
