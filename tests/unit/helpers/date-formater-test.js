import { dateFormater } from '../../../helpers/date-formater';
import { module, test } from 'qunit';

module('Unit | Helper | date formater');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = dateFormater(42);
  assert.ok(result);
});
